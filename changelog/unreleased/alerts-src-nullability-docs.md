type: internal

Src clean-up: `RejectSerializerOptions` declares the null it already tolerates (`Options?`), the three loaders' `IsDryRun` get a real summary (not inherited), and the two extractors' guard-only `ApplyOptions` become static.
